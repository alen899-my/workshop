"use client";

import RepairsClient from "./RepairsClient";

/** Client-side shell — RepairsClient owns all data fetching internally */
export default function RepairsPage() {
  return <RepairsClient />;
}
